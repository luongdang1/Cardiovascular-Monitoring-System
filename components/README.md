# Third-party components

Không còn dùng thư viện Arduino. Các driver (ST7735, MPU6050, MAX30100, GPS parser) đã viết thuần ESP-IDF trong `main/`. Thư mục này hiện không cần thêm gì; nếu muốn bổ sung thư viện ESP-IDF khác, đặt vào đây với `CMakeLists.txt` tương ứng.
